When you hover over any of the lines, the entire line turns gray and an additional button appears. By clicking on this button the user gets a submenu. The problem is that when the user moves the cursor to the submenu, the hover (gray) disappears from the table row.