The config file looks something like the following but repeated 10+ times. I was wondering if I could simplify it to the following. Whether it is best practice, allowed etc? I did not put the topics and queues inside application.properties, because this configuration class is being used by two different projects.