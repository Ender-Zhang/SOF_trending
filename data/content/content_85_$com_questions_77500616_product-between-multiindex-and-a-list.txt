I have a MultiIndex object with 2 levels: I want to multiply it by a list l=[4,5] , I tried I managed to get my expected result with Is there a better way to do this operation ? I think your approach is quite reasonable. Another option using a cross- merge with an intermediate DataFrame format: Output: You can try the below: A possibly more efficient way is to do the combination at an array level, using np.repeat and np.tile , before building a new MultiIndex. This way, you are dealing with the arrays, instead of building a DataFrame only to return to an Index. As always, test with your actual data: