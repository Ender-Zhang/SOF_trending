I want to check how many (if it's like in config) I have disabled elements. In Cypress it'd be like that: but I can't make it work for myself in Playwright I have page with I thought of just filter over... but seems like it doesn't have method filter available to this array of elements You might try using :disabled directly on your CSS selectors, and using :not to invert the condition: With test ids, you can either use [data-test-id="foo"] in the above code, or getByTestId as in the following example: