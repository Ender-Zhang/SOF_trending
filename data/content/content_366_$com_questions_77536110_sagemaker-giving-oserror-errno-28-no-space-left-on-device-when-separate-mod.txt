This looks like an interesting problem. When I am zipping my model and code together(like below), things work fine. On un-zipping, folder structure will be like below: Where code has two files inference.py and requirements.txt As mentioned before, this works fine. Since creating zip of code and model together means that zipping and uploading time is very huge. So I want to de-couple model and code and tried below. Here on unzipping, model.tar.gz will give you stable-diffusion and controlnet . And unzipping sourcedir.tar.gz will give you inference.py and requirements.txt . This is giving following error: