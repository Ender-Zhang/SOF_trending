How to have a split line from the tree's left to the right edge, like the below one using ECharts: Currently, the below HTML and JavaScript code could generate the tree without the split line, but not sure how to add a split line for the tree, similar to the tree's curved lines.
HTML file tree.html : JavaScript file tree.js : Heres a hacky solution: You can add a graph series, make it transparent and use the markLine property to specify coordinates (the series seems to have at minimum one node for the markLine to appear). A drawback would be that the line is bound to its coordinates and wont move according to the tree if its collapsed / extended. Code: