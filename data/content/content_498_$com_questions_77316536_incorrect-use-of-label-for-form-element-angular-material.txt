I'm receiving the following error from Angular Material when I try to use a <mat-label> element inside a simple <mat-form-field> : Here is the problematic code: Here is the rendered component: The component is fully functional and the label is properly growing and shrinking when selecting and deselecting the widget. The problem is with this error popping in my developer console, which I want to discard (does not look very professional). I tried to assign some ids, use different semantic, with no results so far. Any idea on how to clear this error? Even though mat-label should automatically associate with the form control, you can try explicitly setting an id on the mat-select and then use aria-labelledby to associate the label: